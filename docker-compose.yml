services:
  bot:
    build: .
    container_name: tg_spchm_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/bot.db
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
